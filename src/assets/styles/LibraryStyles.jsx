import { css, Global } from "@emotion/react";

const style = css`
  //  NOTE: to show half of the slides
`;

function LibraryStyles() {
  return <Global styles={style} />;
}

export default LibraryStyles;
